<% provide(:title, 'Artigos') %>

<h1>Edição</h1>

<p>
  <strong>Nome:</strong>
  <%= @edition.e_name %>
</p>

<%= link_to 'Editar esta ediçãoooooooo', edit_journal_edition_path(@edition.journal_id, @edition)%> |
<%= link_to 'Remover esta edição e seus artigos', @edition, method: :delete, data: { confirm: 'Você tem certeza que deseja remover esta edição?' } %> |
<%= link_to 'Voltar a lista de edições cadastradas', journal_editions_path(@edition.journal.id) %>

<br><br><br>

<h1>Artigos desta edição</h1>

<table>
  <thead>
    <tr>
      <th>Título</th>
      <th>Resumo</th>
      <th>Url</th>
      <th>Num. Páginas</th>      
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= link_to book.title%></td>
        <td><%= book.abstract%></td>
        <td><%= book.url%></td>
        <td><%= book.num_pages%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Criar um novo Artigo para esta Edição', new_edition_book_path(@edition)%>